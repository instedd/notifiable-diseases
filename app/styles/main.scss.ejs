/* Bootstrap redefined variables */
$screen-sm:                  730px;

$icon-font-path: "../bower_components/bootstrap-sass-official/assets/fonts/bootstrap/";
// bower:scss
@import "bootstrap-sass-official/assets/stylesheets/bootstrap.scss";
// endbower


.browsehappy {
  margin: 0.2em 0;
  background: #ccc;
  color: #000;
  padding: 0.2em 0;
}

/* Bootstrap redefined styles */

body {
  background: #eaeaea;
}
.navbar {
  background: #ffffff;
}
.nav-tabs {
  border-bottom: 1px solid #c6c6c6;
}

.navbar-toggle .icon-bar {
  background: #2A6498;
}

.nav-tabs > li.active > a,
.nav-tabs > li.active > a:hover,
.nav-tabs > li.active > a:focus {
  @include box-shadow(0 -1px 1px 1px rgba(0, 0, 0, 0.1));
  background-color: #eaeaea;
}

.nav > li > a {
  position: relative;
  display: block;
  padding: 5px 15px;
}

.well {
  background-color: #ffffff;
  @include box-shadow(0 1px 1px 1px rgba(0, 0, 0, 0.1));
}


/* Everything but the jumbotron gets side spacing for mobile first views */
.header,
.marketing,
.footer {
  padding-left: 15px;
  padding-right: 15px;
}

/* Custom page header */
.header {
  border-bottom: 1px solid #e5e5e5;

  /* Make the masthead heading the same height as the navigation */
  h3 {
    margin-top: 0;
    margin-bottom: 0;
    line-height: 40px;
    padding-bottom: 19px;
  }
}

/* Custom page footer */
.footer {
  padding-top: 19px;
  color: #777;
  border-top: 1px solid #e5e5e5;
}

.container-narrow > hr {
  margin: 30px 0;
}

/* Main marketing message and sign up button */
.jumbotron {
  text-align: center;
  border-bottom: 1px solid #e5e5e5;

  .btn {
    font-size: 21px;
    padding: 14px 24px;
  }
}

/* Supporting marketing content */
.marketing {
  margin: 40px 0;

  p + h4 {
    margin-top: 28px;
  }
}

/* Responsive: Portrait tablets and up */
@media screen and (min-width: 768px) {
  .container {
    max-width: 730px;
  }

  /* Remove the padding we set earlier */
  .header,
  .marketing,
  .footer {
    padding-left: 0;
    padding-right: 0;
  }
  /* Space out the masthead */
  .header {
    margin-bottom: 30px;
  }
  /* Remove the bottom border on the jumbotron for visual effect */
  .jumbotron {
    border-bottom: 0;
  }
}

.loading {
  display: inline-block;
  width: 20px;
  background: url('../images/loading.gif') no-repeat center;
}

a.editable-click {
  color: black !important;
  text-decoration: none !important;
  border-bottom: 0px !important;
}

.new-report {
  @extend .pull-right;
  margin-top: -25px;
  background-image: url('../images/ic-newreport.png');
  background-repeat: none;
  width: 17px;
  height: 15px;
}

.nd-chart-container {
  position: relative;
  padding-top: 30px;
  min-height: 400px;
  max-height: 400px;

  .actions {
    display: none;
    position: absolute;
    top: 10px;
    right: 30px;
    z-index: 1;
  }

  .content {
    margin-top: 5px;
  }

  .first-in-trendline {
    margin-top: 40px;
  }

  .chart-loading {
    position: absolute;
    opacity: 0.8;
    background: url('../images/loading-large.gif') no-repeat center 180px #ffffff;
    width: 100%;
    height: 100%;
    top: 0px;
    left: 0px;
    z-index: 10;
  }

  &:hover .actions {
    display: block;
  }
}

.nd-map {
  height: 320px;
  cursor: default;

  .nd-map-marker {
    width: 25px !important;
    height: 25px !important;
    margin: -12.5px -12.5px !important;

    &.red    { background-image: url('../images/locationRed.png')    }
    &.yellow { background-image: url('../images/locationYellow.png') }
    &.green  { background-image: url('../images/locationGreen.png')  }
  }
}

.add-new-visualization {
  @include box-shadow(0px -1px 3px 1px rgba(0, 0, 0, 0.2)inset);
  border-radius: 4px;
  padding: 20px;
  background: #eaeaea;

  .add_new {
    display: block;
    height: 360px;
    position: relative;

    &:before {
      content:'';
      display: block;
      background: #aaaaaa;
      color: #eaeaea;
      border-radius:50%;
      height: 50px;
      width: 50px;
      position: absolute;
      left: 50%;
      top: 50%;
      margin: -25px 0 0 -25px;
    }

    &:after {
      content:'+';
      font-size: 50px;
      line-height: 50px;
      font-weight: bold;
      display: block;
      color: #eaeaea;
      position: absolute;
      left: 50%;
      top: 50%;
      margin: -30px 0 0 -15px;
    }

  }
  &:hover {
    background: #e2e2e2;

    .chart_type div {
      background: #e2e2e2;
    }
  }
  .chart_type {
    @include box-sizing(border-box);
    position: absolute;
    top: 40%;
    left: 0;
    width: 100%;
    padding: 0 25px;

    div {
      text-align: center;
      background: #eaeaea;
      font-weight: bold;

      .chart_button {
        background-image: url('../images/chart_icons.png');
        background-repeat: no-repeat;
        display: inline-block;
        width: 58px;
        height: 54px;

        &.trendline          { background-position: 0px 0px }
        &.population_pyramid { background-position: -119px 0px }
        &.map                { background-position: -239px 0px }
      }
    }

  }
}

@mixin filter-icon() {
  background-repeat: no-repeat;
  &.all {
    background-image: url("../images/f_all.png");
  }
  &.date {
    background-image: url("../images/f_date.png");
  }
  &.age.integer {
    background-image: url("../images/f_age.png");
  }
  &.integer {
    background-image: url("../images/f_list.png");
  }
  &.age_group.enum {
    background-image: url("../images/f_age.png");
  }
  &.gender.enum {
    background-image: url("../images/f_gender.png");
  }
  &.ethnicity.enum, &.race_ethnicity.enum, &.race.enum {
    background-image: url("../images/f_ethnicity.png");
  }
  &.condition.enum {
    background-image: url("../images/f_condition.png");
  }
  &.enum {
    background-image: url("../images/f_list.png");
  }
  &.result.enum {
    background-image: url("../images/f_erlenmeyer.png");
  }
  &.location {
    background-image: url("../images/f_location.png");
  }
}

.filters-list {
  @include box-shadow(0 1px 1px 1px rgba(0, 0, 0, 0.1));
  background: #ffffff;
  border-radius: 5px;
  margin-bottom: 15px;

  .filters-header {
    padding: 10px 15px;
    position: relative;

    a {
      font-size: 12px;
      line-height: 20px;
    }

    &:after {
      content: '';
      display: block;
      position: absolute;
      width: 100%;
      height: 7px;
      left: 0;
      bottom: -7px;
      @include gradient-vertical(#e5e5e5, #fcfcfc);
      @include box-sizing(content-box);
    }
  }

  .filter {
    background: url("../images/f_arrow_sep01.png") no-repeat right top;
    padding: 0 10px 0 15px;

    &:after {
      @include transition(opacity ease 0s 0s);
      opacity:0;
    }

    .filter-title {
      padding: 15px 0;
      position: relative;
      cursor: pointer;
      strong {
        padding: 5px 0 5px 30px ;
        @include filter-icon();
      }

      .count {
        @include box-sizing(border-box);
        font-size: 11px;
        line-height: 50px;
        color: #999999;
        text-align: center;
        border-left: 1px solid #cdcdcd;
        border-right: 1px solid #cdcdcd;
        display: block;
        width: 60px;
        height: 49px;
        position: absolute;
        right: 5px;
        top: 1px;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .remove {
        @include border-right-radius(3px);
        @include box-shadow(1px 1px 1px 0px rgba(0, 0, 0, 0.1));
        background: url("../images/ic-delete-black.png") no-repeat center #ffffff;
        width: 32px;
        height: 30px;
        display: none;
        position: absolute;
        right: -30px;
        top: 6px;

        &:hover {
          display:block;
        }
      }

      .selected-desc {
        font-size: 11px;
        display: inline-block;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
        vertical-align: middle;
      }
      @media (min-width: 1230px) {
        .selected-desc {
          width: 40%;
        }
      }
      @media (min-width: 1400px) {
        .selected-desc {
          width: 50%;
        }
      }
      @media (min-width: 1600px) {
        .selected-desc {
          width: 58%;
        }
      }
    }
    &.expanded {
      background-color: #cccccc;
      padding-bottom: 20px;

      .filter-title {
        position: relative;
        margin-bottom: 15px;
        .count {
          border: none;
          color: #333333;
        }
        &:after {
          content: '';
          display: block;
          position: absolute;
          width: 100%;
          height: 7px;
          left: 0;
          bottom: -7px;
          padding: 0 10px 0 15px;
          margin-left: -15px;
          @include gradient-vertical(#b7b7b7, #cccccc);
          @include box-sizing(content-box);
        }
      }

      & + .filter {
        background: url("../images/f_arrow_sep02.png") no-repeat right top;
        &:after {
          opacity: 0 !important;
        }
      }

      &.last {
        margin-bottom: -10px;
      }
      .remove {
        background-color: #cccccc;
      }
      &.empty {

        .count {
          background: none;
          color:#ff6600;
          &:before {
            display:none;
          }
        }
      }
    }

    &:hover .remove {
      display: block;
    }

    &.first {
      background-image: none;

      .filter-title {
        cursor: default;
        span {
          @include border-top-radius(4px);
          line-height: 30px;
          border-top: 1px solid #cdcdcd;
          top: 15px;
          height: 35px;
        }
      }
      + hr {
        margin-top: 0;
      }
    }

    &.last {
      .filter-title .count {
          @include border-bottom-radius(4px);
          border-bottom: 1px solid #cdcdcd;
      }
    }

    .instructions {
      font-size: 12px;
      padding-bottom: 10px;
      color: #666;
    }
    &.empty {

      .count {
        background: #ff6600;
        color: #ffffff;
        border-bottom: 1px solid #ff6600;

        &:before {
          content:'';
          display: block;
          position: absolute;
          left: 50%;
          margin-left: -27px;
          top: 0px;
          width: 0;
          height: 0;
          border-style: solid;
          border-width: 8px 27px 0 27px;
          border-color: #ffffff transparent transparent transparent;
          z-index: 5;
        }
      }
      & + .filter {
        position: relative;
        &:after {
          @include transition(opacity ease-in .5s .15s);
          content:'';
          display: block;
          position: absolute;
          right: 18px;
          top: 0;
          width: 0;
          height: 0;
          border-style: solid;
          border-width: 8px 27px 0 27px;
          border-color: #ff6600 transparent transparent transparent;
          z-index: 5;
          opacity: 1;
        }
      }
    }
  }
  hr {
    margin: 10px 0;
  }

  .add-new-filter {
    font-weight: bold;
    padding: 0 15px 10px 15px;

    a {
      text-decoration: none !important;
      span {
        font-size: 20px;
      }
    }

    div {
      padding-top: 5px;
      a {
        font-weight: normal;
        display: inline-block;
        margin: 5px 10px 5px 0px;
        text-align:center;
        width: 50px;
        vertical-align: top;
        color: #555555;

        i {
          display: inline-block;
          width: 40px;
          height: 40px;
          border-radius: 4px;
          border: 1px solid #eaeaea;
          background-position: center;
          @include filter-icon();
        }

        span {
          font-size: 12px;
          display: block;
          overflow: hidden;
          text-overflow: ellipsis;
          line-height: 12px;
        }
      }
    }
  }
}
.bottom-actions {
  padding-left: 12px;
  div {
    display: block;
    a {
      color: #555555;
      line-height: 24px;
      text-decoration: none !important;
      img {
        vertical-align: middle;
        display: inline-block;
        margin-bottom: 2px;
      }
    }
  }
}
.close {
  border-radius: 50%;
  background: #666666;
  font-size: 12px;
  color: #ffffff !important;
  font-weight: normal;
  width: 15px;
  height: 15px;
  text-align: center;
  line-height: 15px;
  opacity:.85;
  text-shadow: none !important;

  &:hover{
    opacity:1;
  }
}

.depth1 { padding-left: 10px; }
.depth2 { padding-left: 20px; }
.depth3 { padding-left: 30px; }
.depth4 { padding-left: 40px; }
.depth5 { padding-left: 50px; }

.map-config {

  .row {
    margin-bottom: 8px;
  }

  .description {
    margin-bottom: 15px;
  }

  .nd-slider-widget {

    margin-top: 25px;
    margin-bottom: 40px;

    .noUi-base {
      background: #68c04c;
      .noUi-origin {
        &:first-of-type { background: #feb300; }
        &:last-of-type  { background: #FF1C00; }
        .nd-slider-tooltip {
          display: block;
          position: absolute;
          border: 1px solid #D9D9D9;
          font-size: 12px;
          border-radius: 3px;
          background: #fff;
          top: 30px;
          padding: 4px;
          left: -9px;
          text-align: center;
          width: 50px;

          strong {
           display: block;
           padding: 2px;
          }
        }
      }
    }
  }
}

/*Trendline arrows*/
.scrollArrows {
  font-size: 0;
  float: right;

  a {
    display: inline-block;
    vertical-align: middle;
    width: 26px;
    height: 24px;

    &.leftPage {
      background: url("../images/pPage.png") no-repeat;
    }
    &.leftArrow {
      background: url("../images/pStep.png") no-repeat;
    }
    &.rightArrow {
      background: url("../images/nStep.png") no-repeat;
    }
    &.rightPage {
      background: url("../images/nPage.png") no-repeat;
    }
    &.disabled {
      opacity: 0.5;
    }
  }
}


<% if(yeoman.customStyles) { %>
@import "<%= yeoman.customStyles %>";
<% } %>
